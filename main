<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Portfolio</title>
    <style>
        :root {
            --primary-color: #4a6fa5;
            --secondary-color: #166088;
            --accent-color: #4fc3f7;
            --text-color: #333;
            --bg-color: #f9f9f9;
            --card-bg: #ffffff;
            --card-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .dark-mode {
            --primary-color: #6b8cae;
            --secondary-color: #3a7ca5;
            --accent-color: #64b5f6;
            --text-color: #f0f0f0;
            --bg-color: #121212;
            --card-bg: #1e1e1e;
            --card-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--bg-color);
            color: var(--text-color);
            transition: all 0.3s ease;
        }

        header {
            background-color: var(--primary-color);
            color: white;
            padding: 1rem;
            text-align: center;
            position: relative;
        }

        .theme-toggle {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: transparent;
            border: none;
            color: white;
            cursor: pointer;
            font-size: 1.5rem;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        section {
            margin: 2rem 0;
            padding: 2rem;
            background: var(--card-bg);
            border-radius: 8px;
            box-shadow: var(--card-shadow);
            transition: all 0.3s ease;
        }

        h1, h2, h3 {
            color: var(--primary-color);
        }

        .hero {
            text-align: center;
            padding: 3rem 0;
        }

        .hero img {
            width: 300px;
            height: 300px;
            border-radius: 50%;
            object-fit: contain;
            border: 3px solid var(--accent-color);
            margin-bottom: 1rem;
        }

        .skills-grid {
            display: flex;
            justify-content:center;
            gap: 1.5rem;
            margin-top: 2rem;
            margin-left:auto;
            margin-right:auto;
            max-width: 800px;
        }

        .skill-card {
            padding: 1.5rem;
            background: var(--card-bg);
            border-radius: 8px;
            box-shadow: var(--card-shadow);
            transition: transform 0.3s ease;
        }

        .skill-card:hover {
            transform: translateY(-5px);
        }

        .skill-card img {
            width: 100%;
            height: 150px;
            object-fit: cover;
            border-radius: 6px;
            margin-bottom: 1rem;
        }

        .projects-grid {
            display: flex;
            gap: 2rem;
            margin-top: 2rem;
            margin-left:auto;
            margin-right:auto;
            max-width: 800px;
            
        }

        .project-card {
            overflow: hidden;
            border-radius: 8px;
            box-shadow: var(--card-shadow);
            transition: transform 0.3s ease;
        }

        .project-card:hover {
            transform: translateY(-5px);
        }

        .project-card img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .project-info {
            padding: 1.5rem;
            background: var(--card-bg);
        }

        .contact-form {
            display: grid;
            gap: 1rem;
            max-width: 600px;
            margin: 0 auto;

        }

        input, textarea, button {
            padding: 0.8rem;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-family: inherit;
            font-size: 1rem;
            background: var(--card-bg);
            color: var(--text-color);
        }

        button {
            background: var(--primary-color);
            color: white;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        button:hover {
            background: var(--secondary-color);
        }

        footer {
            text-align: center;
            padding: 2rem;
            background: var(--primary-color);
            color: white;
            margin-top: 2rem;
        }

        @media (max-width: 768px) {
            .skills-grid, .projects-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <header>
        <h1>My Portfolio</h1>
        <button class="theme-toggle" id="themeToggle">🌓</button>
    </header>

    <div class="container">
        <section class="hero">
            <img src="Photo.jpg" alt="Professional headshot of portfolio owner standing in modern office with blurred cityscape in background" />
            <h2>Welcome to My Creative Space</h2>
            <p>A passionate developer and designer creating beautiful digital experiences</p>
        </section>

       <section id="about">
    <h2>About Me</h2>
    <div style="display: flex; gap: 2rem; align-items: center;">
        <img src="image.png" 
             alt="Side profile of developer working at desk with dual monitors showing code" 
             style="flex: 1; max-width: 300px; width: 100%; height: auto; border-radius: 8px;" />
        <div style="flex: 2; padding-left: 2rem;">
            <p>A recent Electronics and Communication Engineering graduate with a passion for Web Development, Software Engineering, Embedded Systems, and IoT-driven solutions.</p>
            <p>While not coding, you can find me working with latest technologies, watching movies, and listening to podcasts.</p>
        </div>
    </div>
</section>


        <section id="skills">
            <h2>My Skills</h2>
            <p>Here are some of the technologies and skills I specialize in:</p>
            
            <div class="skills-grid">
                <div class="skill-card">
                    <img src="Frontend.jpg" alt="Modern laptop screen displaying HTML and CSS code with colorful syntax highlighting" />
                    <h3>Frontend Development</h3>
                    <p>HTML5, CSS3, JavaScript, React, Vue.js</p>
                </div>
                <div class="skill-card">
                    <img src="Backend.jpg" alt="Server room with glowing blue lights and network equipment neatly arranged" />
                    <h3>Backend Development</h3>
                    <p>Node.js, Express, MongoDB, PostgreSQL</p>
                </div>
                <!--<div class="skill-card">
                    <img src="https://placehold.co/400x200" alt="Designer's hand sketching wireframes on paper with markers and drafting tools" />
                    <h3>UI/UX Design</h3>
                    <p>Figma, Adobe XD, Prototyping, User Research</p>
                </div>
                <div class="skill-card">
                    <img src="https://placehold.co/400x200" alt="Mobile phones displaying various versions of the same app interface with responsive layouts" />
                    <h3>Mobile Development</h3>
                    <p>React Native, Flutter, Progressive Web Apps</p>
                </div>-->
            </div>
        </section>

        <section id="projects">
            <h2>Featured Projects</h2>
            <p>Some of my most recent and notable works:</p>
            
            <div class="projects-grid">
                <div class="project-card">
                    <img src="Screenshot 2025-07-12 005624.png" alt="Website dashboard showing analytics charts and metrics with a clean modern design" />
                    <div class="project-info">
                        <h3>Web Page</h3>
                        <p>A responsive dashboard for tracking business metrics with real-time data visualization.</p>
                    </div>
                </div>
                <div class="project-card">
                    <img src="UGV_TALON.jpg" alt="Mobile app screens showing step-by-step cooking tutorial with colorful food photography" />
                    <div class="project-info">
                        <h3>Ai Driven Military Robot</h3>
                        <p>An interactive device with personalized for military by image recoginition.</p>
                    </div>
                </div>
                <!--<div class="project-card">
                    <img src="https://placehold.co/600x400" alt="E-commerce website product page with clean layout showcasing electronics and accessories" />
                    <div class="project-info">
                        <h3>E-Commerce Platform</h3>
                        <p>A full-featured online store with inventory management and secure payment processing.</p>
                    </div>
                </div>-->
            </div>
        </section>

        <section id="contact-form">
            <h1>Contact Information</h1>
            <p>Gopisaravanan K</p>
            <p>Email: gopisudha1112@gmail.com</p>
            <p>Phone: +91 88256 92326</p>
            <div><a href="Resume_.pdf">----click here to check resume----</a></div>
            <p><h2>If you'd like to get in touch, please fill out the form below or reach me via email.</h2></p>
            <form class="contact-form">
                <input type="text" name="name" placeholder="Your Name" required>
                <input type="email" name="email" placeholder="Your Email" required>
                <textarea name="message" rows="5" placeholder="Your Message" required></textarea>
                <button type="submit">Send Message</button>
        </section>
    </div>

    <footer>
        <p>&copy; 2025 My Portfolio. All rights reserved.</p>
        <p>Find me on: <a href="#" style="color: var(--accent-color);">LinkedIn</a> | <a href="https://www.instagram.com/peace_maker_1103/" style="color: var(--accent-color);">Instagram</a> | <a href="#" style="color: var(--accent-color);">Facebook</a></p>
    </footer>

    <script>
        // Theme toggle functionality
        const themeToggle = document.getElementById('themeToggle');
        const prefersDarkScheme = window.matchMedia('(prefers-color-scheme: dark)');
        
        // Check for saved theme preference or use system preference
        const currentTheme = localStorage.getItem('theme') || 
                            (prefersDarkScheme.matches ? 'dark' : 'light');
        
        if (currentTheme === 'dark') {
            document.body.classList.add('dark-mode');
            themeToggle.textContent = '🌕';
        } else {
            themeToggle.textContent = '🌓';
        }

        // Toggle theme on button click
        themeToggle.addEventListener('click', () => {
            document.body.classList.toggle('dark-mode');
            const theme = document.body.classList.contains('dark-mode') ? 'dark' : 'light';
            localStorage.setItem('theme', theme);
            themeToggle.textContent = theme === 'dark' ? '🌕' : '🌓';
        });

        // Form submission handler
        const form = document.querySelector('.contact-form');
        form.addEventListener('submit', (e) => {
            e.preventDefault();
            alert('Thank you for your message! I will get back to you soon.');
            form.reset();
        });

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                document.querySelector(this.getAttribute('href')).scrollIntoView({
                    behavior: 'smooth'
                });
            });
        });
    </script>
</body>
</html>
